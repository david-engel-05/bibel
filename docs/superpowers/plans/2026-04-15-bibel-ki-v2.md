# Bibel KI v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bibel KI von einer simplen Q&A-Seite zu einer vollständigen Chat-App mit RAG (pgvector), anonymen Sessions und Streaming ausbauen.

**Architecture:** FastAPI-Backend bekommt drei Endpunkte (`/session`, `/history`, `/ask`-Stream) und nutzt Supabase pgvector für semantische Bibelsuche. Das Frontend (Next.js) wird zu einer Chat-UI mit localStorage-Session und live Streaming umgebaut. Ein einmaliges Import-Skript befüllt die Vektordatenbank aus `bible.json`.

**Tech Stack:** FastAPI, Supabase (pgvector), Ollama (`nomic-embed-text` + `gemma4:26b`), Next.js 16, React 19, Tailwind CSS v4, pytest + httpx

---

## Dateistruktur

```
bibel/
├── supabase/
│   └── schema.sql                  NEU  — DDL für alle Tabellen + RPC-Funktion
├── backend/
│   ├── .env.example                NEU  — Env-Var-Vorlage
│   ├── database.py                 NEU  — Supabase-Client (lazy, DI-fähig)
│   ├── import_bible.py             NEU  — Einmaliges Embed+Import-Skript
│   ├── main.py                     MOD  — Alte /ask ersetzen, /session + /history hinzufügen
│   └── tests/
│       ├── __init__.py             NEU
│       └── test_endpoints.py       NEU  — pytest für alle drei Endpunkte
└── frontend/
    └── app/
        └── page.tsx                MOD  — Komplett als Chat-UI neu schreiben
```

---

## Task 1: Supabase-Schema anlegen

**Files:**
- Create: `supabase/schema.sql`

- [ ] **Schritt 1: SQL-Datei erstellen**

```sql
-- supabase/schema.sql
-- Im Supabase SQL Editor ausführen (Dashboard → SQL Editor → New query)

-- pgvector aktivieren
create extension if not exists vector;

-- Bibelverse mit Embeddings
create table if not exists bible_verses (
  id        bigserial primary key,
  book      text    not null,
  chapter   int     not null,
  verse     int     not null,
  text      text    not null,
  embedding vector(768)
);

-- Anonyme Sessions
create table if not exists chat_sessions (
  id         uuid primary key default gen_random_uuid(),
  created_at timestamptz default now()
);

-- Gesprächsnachrichten
create table if not exists chat_messages (
  id         bigserial primary key,
  session_id uuid not null references chat_sessions(id) on delete cascade,
  role       text not null check (role in ('user', 'assistant')),
  content    text not null,
  created_at timestamptz default now()
);

-- RPC-Funktion für Vektorsuche (wird nach dem Import mit Index ergänzt)
create or replace function match_verses(
  query_embedding vector(768),
  match_count     int default 5
)
returns table (
  id      bigint,
  book    text,
  chapter int,
  verse   int,
  text    text
)
language sql stable
as $$
  select id, book, chapter, verse, text
  from bible_verses
  order by embedding <=> query_embedding
  limit match_count;
$$;
```

- [ ] **Schritt 2: Im Supabase Dashboard ausführen**

  1. Supabase-Projekt anlegen (falls noch nicht vorhanden): https://supabase.com/dashboard
  2. Sicherstellen dass unter **Database → Extensions** `vector` aktiviert ist
  3. Zu **SQL Editor → New query** navigieren, den SQL-Inhalt einfügen und auf **Run** klicken
  4. Prüfen: unter **Table Editor** sollen `bible_verses`, `chat_sessions`, `chat_messages` erscheinen

- [ ] **Schritt 3: API-Credentials notieren**

  Im Supabase Dashboard unter **Settings → API**:
  - `Project URL` → wird zu `SUPABASE_URL`
  - `service_role` key (nicht `anon`) → wird zu `SUPABASE_KEY`

- [ ] **Schritt 4: Commit**

```bash
git add supabase/schema.sql
git commit -m "feat: add supabase schema for bible_verses, chat_sessions, chat_messages"
```

---

## Task 2: Backend-Umgebung einrichten

**Files:**
- Create: `backend/.env.example`
- Create: `backend/database.py`
- Modify: `backend/main.py` (Imports + bible.json-Zeilen entfernen)

- [ ] **Schritt 1: Abhängigkeiten installieren**

```bash
cd backend
source venv/bin/activate
pip install python-dotenv tqdm pytest httpx pytest-mock
```

Erwartete Ausgabe: `Successfully installed python-dotenv-... tqdm-... pytest-... httpx-... pytest-mock-...`

- [ ] **Schritt 2: `.env.example` erstellen**

```
# backend/.env.example
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-service-role-key
```

- [ ] **Schritt 3: `.env` aus Vorlage anlegen und befüllen**

```bash
cp backend/.env.example backend/.env
# Dann die echten Werte aus Task 1 eintragen
```

- [ ] **Schritt 4: `backend/database.py` erstellen**

```python
# backend/database.py
import os
from supabase import create_client, Client


def get_supabase() -> Client:
    return create_client(
        os.environ["SUPABASE_URL"],
        os.environ["SUPABASE_KEY"],
    )
```

- [ ] **Schritt 5: `backend/main.py` bereinigen**

Die folgenden Zeilen aus `main.py` entfernen (bible.json-Laden + altes Question-Modell):

```python
# ENTFERNEN:
import json

# ENTFERNEN:
with open("bible.json", "r", encoding="utf-8") as f:
    bible = json.load(f)

# ENTFERNEN:
class Question(BaseModel):
    question: str

# ENTFERNEN (gesamte Funktion):
@app.post("/ask")
def ask(q: Question):
    ...
```

Neue Imports oben in `main.py` einfügen:

```python
from fastapi import FastAPI, Depends
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import StreamingResponse
from pydantic import BaseModel
from supabase import Client
import ollama
from dotenv import load_dotenv
from database import get_supabase

load_dotenv()
```

- [ ] **Schritt 6: Commit**

```bash
git add backend/.env.example backend/database.py backend/main.py
git commit -m "feat: add database module and clean up main.py"
```

---

## Task 3: Bible-Import-Skript

**Files:**
- Create: `backend/import_bible.py`

- [ ] **Schritt 1: `backend/import_bible.py` erstellen**

```python
# backend/import_bible.py
"""
Einmaliges Skript: bible.json → Supabase bible_verses mit Embeddings.
Laufzeit: ~2-4h für ~31.000 Verse (lokal, Ollama nomic-embed-text).
Ausführen: cd backend && source venv/bin/activate && python import_bible.py
"""
import json
import sys
from dotenv import load_dotenv
from tqdm import tqdm
import ollama
from database import get_supabase

load_dotenv()

BATCH_SIZE = 50


def load_verses(path: str) -> list[dict]:
    with open(path, "r", encoding="utf-8") as f:
        data = json.load(f)
    verses = []
    for book in data["books"]:
        for chapter in book["chapters"]:
            for v in chapter["verses"]:
                verses.append({
                    "book": book["name"],
                    "chapter": chapter["chapter"],
                    "verse": v["verse"],
                    "text": v["text"].strip(),
                })
    return verses


def embed(text: str) -> list[float]:
    result = ollama.embed(model="nomic-embed-text", input=text)
    return result.embeddings[0]


def main():
    # Sicherstellen dass nomic-embed-text vorhanden ist
    print("Prüfe nomic-embed-text Modell...")
    try:
        embed("test")
    except Exception as e:
        print(f"Fehler: {e}")
        print("Bitte zuerst ausführen: ollama pull nomic-embed-text")
        sys.exit(1)

    db = get_supabase()
    verses = load_verses("bible.json")
    print(f"{len(verses)} Verse geladen")

    for i in tqdm(range(0, len(verses), BATCH_SIZE), desc="Importiere"):
        batch = verses[i : i + BATCH_SIZE]
        rows = []
        for v in batch:
            rows.append({
                "book": v["book"],
                "chapter": v["chapter"],
                "verse": v["verse"],
                "text": v["text"],
                "embedding": embed(v["text"]),
            })
        db.table("bible_verses").insert(rows).execute()

    print("Import abgeschlossen! Jetzt den ivfflat-Index im Supabase SQL Editor anlegen (siehe Task 3 Schritt 2).")


if __name__ == "__main__":
    main()
```

- [ ] **Schritt 2: nomic-embed-text pullen**

```bash
ollama pull nomic-embed-text
```

Erwartete Ausgabe: `pulling manifest ... success`

- [ ] **Schritt 3: Import-Skript testen (Dry-run mit 5 Versen)**

Temporär in `import_bible.py` Zeile `verses = load_verses(...)` durch Folgendes ersetzen um nur 5 Verse zu importieren:

```python
verses = load_verses("bible.json")[:5]
```

Dann ausführen:

```bash
cd backend && source venv/bin/activate && python import_bible.py
```

Erwartete Ausgabe:
```
Prüfe nomic-embed-text Modell...
5 Verse geladen
Importiere: 100%|████| 1/1
Import abgeschlossen!
```

Im Supabase Table Editor prüfen: `bible_verses` soll 5 Zeilen mit gefülltem `embedding`-Feld enthalten.

- [ ] **Schritt 4: Testdaten löschen und vollständigen Import starten**

Im Supabase SQL Editor:
```sql
truncate table bible_verses;
```

Dann in `import_bible.py` die `[:5]`-Einschränkung wieder entfernen und den Import im Hintergrund starten:

```bash
cd backend && source venv/bin/activate && nohup python import_bible.py > import.log 2>&1 &
echo "Import läuft im Hintergrund, Fortschritt: tail -f backend/import.log"
```

- [ ] **Schritt 5: ivfflat-Index nach abgeschlossenem Import anlegen**

Im Supabase SQL Editor ausführen (erst wenn Import vollständig!):

```sql
create index if not exists bible_verses_embedding_idx
  on bible_verses
  using ivfflat (embedding vector_cosine_ops)
  with (lists = 100);
```

- [ ] **Schritt 6: Commit**

```bash
git add backend/import_bible.py
git commit -m "feat: add bible import script with pgvector embeddings"
```

---

## Task 4: `/session`-Endpunkt (TDD)

**Files:**
- Create: `backend/tests/__init__.py`
- Create: `backend/tests/test_endpoints.py`
- Modify: `backend/main.py`

- [ ] **Schritt 1: Test-Verzeichnis anlegen**

```bash
touch backend/tests/__init__.py
```

- [ ] **Schritt 2: Failing-Test schreiben**

```python
# backend/tests/test_endpoints.py
import pytest
from unittest.mock import MagicMock
from fastapi.testclient import TestClient


def make_mock_db():
    """Hilfsfunktion: gibt einen vorkonfigurierten Supabase-Mock zurück."""
    db = MagicMock()
    return db


# --- /session ---

def test_create_session_returns_uuid():
    mock_db = make_mock_db()
    mock_db.table.return_value.insert.return_value.execute.return_value = MagicMock(
        data=[{"id": "123e4567-e89b-12d3-a456-426614174000"}]
    )

    import os
    os.environ.setdefault("SUPABASE_URL", "https://test.supabase.co")
    os.environ.setdefault("SUPABASE_KEY", "test-key")

    from main import app, get_supabase
    app.dependency_overrides[get_supabase] = lambda: mock_db
    client = TestClient(app)

    response = client.post("/session")

    assert response.status_code == 200
    assert response.json() == {"session_id": "123e4567-e89b-12d3-a456-426614174000"}
    mock_db.table.assert_called_once_with("chat_sessions")
    app.dependency_overrides.clear()
```

- [ ] **Schritt 3: Test ausführen und sicherstellen dass er scheitert**

```bash
cd backend && source venv/bin/activate && pytest tests/test_endpoints.py::test_create_session_returns_uuid -v
```

Erwartetes Ergebnis: `FAILED` — `/session`-Route existiert noch nicht.

- [ ] **Schritt 4: `/session`-Endpunkt in `main.py` implementieren**

In `main.py` nach dem Middleware-Block einfügen:

```python
@app.post("/session")
def create_session(db: Client = Depends(get_supabase)):
    result = db.table("chat_sessions").insert({}).execute()
    return {"session_id": result.data[0]["id"]}
```

- [ ] **Schritt 5: Test ausführen und sicherstellen dass er besteht**

```bash
cd backend && source venv/bin/activate && pytest tests/test_endpoints.py::test_create_session_returns_uuid -v
```

Erwartetes Ergebnis: `PASSED`

- [ ] **Schritt 6: Commit**

```bash
git add backend/main.py backend/tests/__init__.py backend/tests/test_endpoints.py
git commit -m "feat: add /session endpoint with test"
```

---

## Task 5: `/history`-Endpunkt (TDD)

**Files:**
- Modify: `backend/tests/test_endpoints.py`
- Modify: `backend/main.py`

- [ ] **Schritt 1: Failing-Test zu `test_endpoints.py` hinzufügen**

An das Ende von `test_endpoints.py` anhängen:

```python
# --- /history ---

def test_get_history_returns_messages():
    mock_db = make_mock_db()
    mock_db.table.return_value.select.return_value.eq.return_value.order.return_value.execute.return_value = MagicMock(
        data=[
            {"role": "user", "content": "Hallo", "created_at": "2026-04-15T10:00:00Z"},
            {"role": "assistant", "content": "Antwort", "created_at": "2026-04-15T10:00:01Z"},
        ]
    )

    import os
    os.environ.setdefault("SUPABASE_URL", "https://test.supabase.co")
    os.environ.setdefault("SUPABASE_KEY", "test-key")

    from main import app, get_supabase
    app.dependency_overrides[get_supabase] = lambda: mock_db
    client = TestClient(app)

    session_id = "123e4567-e89b-12d3-a456-426614174000"
    response = client.get(f"/history/{session_id}")

    assert response.status_code == 200
    data = response.json()
    assert len(data) == 2
    assert data[0]["role"] == "user"
    assert data[1]["role"] == "assistant"
    app.dependency_overrides.clear()


def test_get_history_empty_for_new_session():
    mock_db = make_mock_db()
    mock_db.table.return_value.select.return_value.eq.return_value.order.return_value.execute.return_value = MagicMock(
        data=[]
    )

    import os
    os.environ.setdefault("SUPABASE_URL", "https://test.supabase.co")
    os.environ.setdefault("SUPABASE_KEY", "test-key")

    from main import app, get_supabase
    app.dependency_overrides[get_supabase] = lambda: mock_db
    client = TestClient(app)

    response = client.get("/history/neue-session-id")

    assert response.status_code == 200
    assert response.json() == []
    app.dependency_overrides.clear()
```

- [ ] **Schritt 2: Tests ausführen und sicherstellen dass sie scheitern**

```bash
cd backend && source venv/bin/activate && pytest tests/test_endpoints.py::test_get_history_returns_messages tests/test_endpoints.py::test_get_history_empty_for_new_session -v
```

Erwartetes Ergebnis: `FAILED` (Route nicht gefunden)

- [ ] **Schritt 3: `/history`-Endpunkt in `main.py` implementieren**

In `main.py` nach `/session` einfügen:

```python
@app.get("/history/{session_id}")
def get_history(session_id: str, db: Client = Depends(get_supabase)):
    result = (
        db.table("chat_messages")
        .select("role, content, created_at")
        .eq("session_id", session_id)
        .order("created_at")
        .execute()
    )
    return result.data
```

- [ ] **Schritt 4: Tests ausführen und sicherstellen dass sie bestehen**

```bash
cd backend && source venv/bin/activate && pytest tests/test_endpoints.py -v
```

Erwartetes Ergebnis: alle 3 Tests `PASSED`

- [ ] **Schritt 5: Commit**

```bash
git add backend/main.py backend/tests/test_endpoints.py
git commit -m "feat: add /history endpoint with tests"
```

---

## Task 6: `/ask`-Streaming-Endpunkt (TDD)

**Files:**
- Modify: `backend/tests/test_endpoints.py`
- Modify: `backend/main.py`

- [ ] **Schritt 1: Datenmodell in `main.py` hinzufügen**

In `main.py` nach den Imports einfügen:

```python
class AskRequest(BaseModel):
    question: str
    session_id: str
```

- [ ] **Schritt 2: Failing-Test zu `test_endpoints.py` hinzufügen**

An das Ende von `test_endpoints.py` anhängen:

```python
# --- /ask ---

def test_ask_streams_response(mocker):
    mock_db = make_mock_db()

    # Verse-Suche gibt 2 Verse zurück
    mock_db.rpc.return_value.execute.return_value = MagicMock(
        data=[
            {"book": "Johannes", "chapter": 3, "verse": 16, "text": "Denn also hat Gott..."},
            {"book": "Römer", "chapter": 8, "verse": 28, "text": "Wir wissen aber..."},
        ]
    )
    # chat_messages.insert gibt keine relevante Antwort
    mock_db.table.return_value.insert.return_value.execute.return_value = MagicMock(data=[])

    # Ollama-Embedding mocken
    mock_embed = mocker.patch("main.ollama.embed")
    mock_embed.return_value = MagicMock(embeddings=[[0.1] * 768])

    # Ollama-Chat-Stream mocken: liefert 3 Chunks
    def fake_chat(**kwargs):
        for token in ["Gott ", "liebt ", "dich."]:
            yield MagicMock(message=MagicMock(content=token))

    mocker.patch("main.ollama.chat", side_effect=fake_chat)

    import os
    os.environ.setdefault("SUPABASE_URL", "https://test.supabase.co")
    os.environ.setdefault("SUPABASE_KEY", "test-key")

    from main import app, get_supabase
    app.dependency_overrides[get_supabase] = lambda: mock_db
    client = TestClient(app)

    response = client.post(
        "/ask",
        json={"question": "Was bedeutet Liebe?", "session_id": "test-session-id"},
    )

    assert response.status_code == 200
    assert "text/event-stream" in response.headers["content-type"]
    body = response.text
    assert "data: Gott " in body
    assert "data: liebt " in body
    assert "data: dich." in body
    assert "data: [DONE]" in body
    app.dependency_overrides.clear()
```

- [ ] **Schritt 3: Test ausführen und sicherstellen dass er scheitert**

```bash
cd backend && source venv/bin/activate && pytest tests/test_endpoints.py::test_ask_streams_response -v
```

Erwartetes Ergebnis: `FAILED` (Route nicht gefunden)

- [ ] **Schritt 4: `/ask`-Endpunkt in `main.py` implementieren**

In `main.py` nach `/history` einfügen:

```python
@app.post("/ask")
def ask(req: AskRequest, db: Client = Depends(get_supabase)):
    # 1. Frage einbetten
    embed_result = ollama.embed(model="nomic-embed-text", input=req.question)
    question_embedding = embed_result.embeddings[0]

    # 2. Top-5 semantisch ähnliche Verse aus Supabase laden
    verses_result = db.rpc(
        "match_verses",
        {"query_embedding": question_embedding, "match_count": 5},
    ).execute()
    context = "\n".join(
        f"{v['book']} {v['chapter']}:{v['verse']} — {v['text']}"
        for v in verses_result.data
    )

    # 3. Nutzernachricht speichern
    db.table("chat_messages").insert(
        {"session_id": req.session_id, "role": "user", "content": req.question}
    ).execute()

    # 4. Stream generieren und Antwort anschließend persistieren
    full_response: list[str] = []

    def generate():
        for chunk in ollama.chat(
            model="gemma4:26b",
            messages=[
                {
                    "role": "system",
                    "content": (
                        "Du bist ein hilfreicher Bibel-Assistent.\n"
                        "Beantworte Fragen auf Deutsch basierend auf der Bibel.\n"
                        "Gib immer die genaue Bibelstelle an (z.B. Johannes 3:16).\n"
                        "Sei freundlich und verständlich.\n\n"
                        f"Relevante Bibelstellen:\n{context}"
                    ),
                },
                {"role": "user", "content": req.question},
            ],
            stream=True,
        ):
            token = chunk.message.content
            if token:
                full_response.append(token)
                yield f"data: {token}\n\n"

        db.table("chat_messages").insert(
            {
                "session_id": req.session_id,
                "role": "assistant",
                "content": "".join(full_response),
            }
        ).execute()
        yield "data: [DONE]\n\n"

    return StreamingResponse(generate(), media_type="text/event-stream")
```

- [ ] **Schritt 5: Alle Tests ausführen**

```bash
cd backend && source venv/bin/activate && pytest tests/ -v
```

Erwartetes Ergebnis:
```
tests/test_endpoints.py::test_create_session_returns_uuid PASSED
tests/test_endpoints.py::test_get_history_returns_messages PASSED
tests/test_endpoints.py::test_get_history_empty_for_new_session PASSED
tests/test_endpoints.py::test_ask_streams_response PASSED
4 passed
```

- [ ] **Schritt 6: Backend manuell testen**

```bash
cd backend && source venv/bin/activate && python main.py
```

In einem zweiten Terminal:
```bash
# Session anlegen
curl -s -X POST http://localhost:8000/session | python3 -m json.tool

# Streaming-Test (SESSION_ID aus dem vorherigen Schritt ersetzen)
curl -s -N -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d '{"question":"Was bedeutet Liebe in der Bibel?","session_id":"SESSION_ID"}'
```

Erwartete Ausgabe: SSE-Tokens erscheinen fortlaufend, endet mit `data: [DONE]`

- [ ] **Schritt 7: Commit**

```bash
git add backend/main.py backend/tests/test_endpoints.py
git commit -m "feat: add streaming /ask endpoint with RAG and tests"
```

---

## Task 7: Frontend Chat-UI

**Files:**
- Modify: `frontend/app/page.tsx`

- [ ] **Schritt 1: `@tailwindcss/typography` in Tailwind v4 aktivieren**

In `frontend/app/globals.css` die Zeile `@import "tailwindcss"` prüfen. Falls vorhanden, direkt darunter einfügen:

```css
@plugin "@tailwindcss/typography";
```

- [ ] **Schritt 2: `frontend/app/page.tsx` komplett ersetzen**

```typescript
"use client";
import { useState, useEffect, useRef } from "react";
import ReactMarkdown from "react-markdown";

type Message = {
  role: "user" | "assistant";
  content: string;
};

const API = "http://localhost:8000";

export default function Home() {
  const [messages, setMessages] = useState<Message[]>([]);
  const [input, setInput] = useState("");
  const [streaming, setStreaming] = useState(false);
  const [sessionId, setSessionId] = useState<string | null>(null);
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    initSession();
  }, []);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: "smooth" });
  }, [messages]);

  async function initSession() {
    let id = localStorage.getItem("session_id");
    if (!id) {
      const res = await fetch(`${API}/session`, { method: "POST" });
      const data = await res.json();
      id = data.session_id as string;
      localStorage.setItem("session_id", id);
    }
    setSessionId(id);
    const histRes = await fetch(`${API}/history/${id}`);
    const history: { role: "user" | "assistant"; content: string }[] =
      await histRes.json();
    setMessages(history.map((m) => ({ role: m.role, content: m.content })));
  }

  async function newChat() {
    const res = await fetch(`${API}/session`, { method: "POST" });
    const data = await res.json();
    const id = data.session_id as string;
    localStorage.setItem("session_id", id);
    setSessionId(id);
    setMessages([]);
  }

  async function send() {
    if (!input.trim() || streaming || !sessionId) return;
    const question = input.trim();
    setInput("");
    setMessages((prev) => [...prev, { role: "user", content: question }]);
    setMessages((prev) => [...prev, { role: "assistant", content: "" }]);
    setStreaming(true);

    const res = await fetch(`${API}/ask`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ question, session_id: sessionId }),
    });

    const reader = res.body!.getReader();
    const decoder = new TextDecoder();

    outer: while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      const chunk = decoder.decode(value);
      for (const line of chunk.split("\n")) {
        if (!line.startsWith("data: ")) continue;
        const token = line.slice(6);
        if (token === "[DONE]") break outer;
        setMessages((prev) => {
          const last = prev[prev.length - 1];
          return [
            ...prev.slice(0, -1),
            { ...last, content: last.content + token },
          ];
        });
      }
    }
    setStreaming(false);
  }

  return (
    <div className="flex flex-col h-screen bg-gray-950 text-white">
      {/* Header */}
      <header className="flex items-center justify-between px-6 py-4 border-b border-gray-800 shrink-0">
        <h1 className="text-xl font-bold">Bibel KI</h1>
        <button
          onClick={newChat}
          className="text-sm text-gray-400 hover:text-white px-3 py-1 rounded border border-gray-700 hover:border-gray-500 transition-colors"
        >
          Neuer Chat
        </button>
      </header>

      {/* Nachrichten */}
      <div className="flex-1 overflow-y-auto px-4 py-6 space-y-4">
        {messages.length === 0 && (
          <p className="text-center text-gray-500 mt-24 text-sm">
            Stelle eine Frage zur Bibel
          </p>
        )}
        {messages.map((msg, i) => (
          <div
            key={i}
            className={`flex ${msg.role === "user" ? "justify-end" : "justify-start"}`}
          >
            <div
              className={`max-w-2xl px-4 py-3 rounded-2xl ${
                msg.role === "user"
                  ? "bg-blue-600 text-white"
                  : "bg-gray-800 text-gray-100"
              }`}
            >
              {msg.role === "assistant" ? (
                <div className="prose prose-invert prose-sm max-w-none">
                  <ReactMarkdown>
                    {msg.content ||
                      (streaming && i === messages.length - 1 ? "▋" : "")}
                  </ReactMarkdown>
                </div>
              ) : (
                msg.content
              )}
            </div>
          </div>
        ))}
        <div ref={bottomRef} />
      </div>

      {/* Eingabe */}
      <div className="border-t border-gray-800 px-4 py-4 shrink-0">
        <div className="flex gap-2 max-w-3xl mx-auto">
          <input
            className="flex-1 bg-gray-800 rounded-xl px-4 py-3 outline-none focus:ring-2 focus:ring-blue-500 disabled:opacity-50"
            placeholder="z.B. Was bedeutet Johannes 3:16?"
            value={input}
            onChange={(e) => setInput(e.target.value)}
            onKeyDown={(e) => e.key === "Enter" && !e.shiftKey && send()}
            disabled={streaming}
          />
          <button
            onClick={send}
            disabled={streaming || !input.trim()}
            className="bg-blue-600 hover:bg-blue-700 disabled:opacity-40 disabled:cursor-not-allowed px-5 py-3 rounded-xl font-medium transition-colors"
          >
            {streaming ? "..." : "Senden"}
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Schritt 3: Frontend starten und testen**

```bash
cd frontend && npm run dev
```

Öffne http://localhost:3000 und prüfe:
1. Beim ersten Laden erscheint ein leerer Chat mit dem Platzhaltertext
2. Eine Frage stellen → Antwort erscheint als Blase und wird live getippt
3. Seite neu laden → Verlauf ist noch vorhanden
4. „Neuer Chat"-Button → leerer Chat, neue Session

- [ ] **Schritt 4: TypeScript-Check**

```bash
cd frontend && npx tsc --noEmit
```

Erwartetes Ergebnis: keine Fehler

- [ ] **Schritt 5: Commit**

```bash
git add frontend/app/page.tsx frontend/app/globals.css
git commit -m "feat: rewrite frontend as streaming chat UI with session history"
```

---

## Task 8: Abschluss

- [ ] **Schritt 1: `.gitignore` erweitern**

Zur `.gitignore` (oder Root-Ebene anlegen) hinzufügen:

```gitignore
backend/.env
backend/import.log
backend/venv/
.superpowers/
```

- [ ] **Schritt 2: CLAUDE.md aktualisieren**

In `/CLAUDE.md` den Abschnitt „Architektur" um Supabase und Embeddings ergänzen:

```markdown
## Architektur

Drei Komponenten:

```
backend/   FastAPI (Python) — localhost:8000
frontend/  Next.js (TypeScript) — localhost:3000
Supabase   pgvector (bible_verses) + chat_sessions + chat_messages
```

**Request flow:** `page.tsx` → `POST /ask` (SSE) → FastAPI → Ollama `nomic-embed-text` (Embedding) → Supabase pgvector (Top-5 Verse) → Ollama `gemma4:26b` (Stream) → SSE-Tokens an Browser.

**Einmaliger Setup:** `backend/import_bible.py` befüllt `bible_verses` mit Embeddings (~31.000 Verse, ~2–4h).
```

- [ ] **Schritt 3: Alle Tests ein letztes Mal ausführen**

```bash
cd backend && source venv/bin/activate && pytest tests/ -v
```

Erwartetes Ergebnis: 4 passed, 0 failed

- [ ] **Schritt 4: Final-Commit**

```bash
git add .gitignore CLAUDE.md
git commit -m "chore: update gitignore and CLAUDE.md for v2 architecture"
```
